---
/**
 * Pure pass-through wrapper. Renders its children unchanged on the
 * web. The audio preprocessor (scripts/blog-audio/preprocess.ts)
 * recognizes the component name and drops the entire subtree from
 * narration.
 */
---

<slot />
